% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{hbb}
\alias{hbb}
\title{Fit a Hurdle Beta-Binomial Model}
\usage{
hbb(
  formula,
  data,
  weights = NULL,
  stratum = NULL,
  psu = NULL,
  state_data = NULL,
  prior = default_prior(),
  chains = 4L,
  iter_warmup = 1000L,
  iter_sampling = 1000L,
  adapt_delta = 0.95,
  max_treedepth = 12L,
  seed = NULL,
  refresh = NULL,
  cpp_options = list(),
  ...
)
}
\arguments{
\item{formula}{A two-sided formula of the form
\code{y | trials(n) ~ predictors}, or an object of class
\link{hbb_formula}. See \code{\link[=hbb_formula]{hbb_formula()}} for the full syntax including
random effects and policy moderators.}

\item{data}{A data frame containing provider-level variables: the
response, trials, fixed effects, and optionally the grouping
variable.}

\item{weights}{Character string naming the column in \code{data}
containing survey sampling weights, or \code{NULL} (default) for
unweighted analysis.}

\item{stratum}{Character string naming the column in \code{data}
containing sampling stratum identifiers, or \code{NULL}.}

\item{psu}{Character string naming the column in \code{data} containing
primary sampling unit (PSU) identifiers, or \code{NULL}.}

\item{state_data}{A data frame of group-level (state-level) variables
for policy moderators. Required if the formula includes
\code{state_level()} terms. Must contain a column matching the grouping
variable for merging.}

\item{prior}{A prior specification object of class \link{hbb_prior}, or
\code{NULL} to use \code{\link[=default_prior]{default_prior()}}. See \code{\link[=hbb_prior]{hbb_prior()}} for
customisation.}

\item{chains}{Integer. Number of Markov chains. Default \code{4}.}

\item{iter_warmup}{Integer. Number of warmup iterations per chain.
Default \code{1000}.}

\item{iter_sampling}{Integer. Number of post-warmup iterations per
chain. Default \code{1000}.}

\item{adapt_delta}{Numeric in \verb{(0, 1)}. Target average proposal
acceptance probability during warmup adaptation. Higher values
reduce divergent transitions at the cost of slower sampling.
Default \code{0.95}.}

\item{max_treedepth}{Integer. Maximum tree depth for the NUTS
sampler. Default \code{12}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{refresh}{Integer or \code{NULL}. How often to print progress (every
\code{refresh} iterations). Set to \code{0} to suppress progress output.
\code{NULL} uses the CmdStan default.}

\item{cpp_options}{Named list of C++ compilation options passed to
\code{cmdstanr::cmdstan_model()}. For example,
\code{list(stan_threads = TRUE)} enables within-chain threading.
Default \code{list()}.}

\item{...}{Additional arguments passed to the CmdStanModel
\verb{$sample()} method.}
}
\value{
An S3 object of class \code{"hbb_fit"}. See \code{\link[=is.hbb_fit]{is.hbb_fit()}} and
\code{\link[=print.hbb_fit]{print.hbb_fit()}} for methods. The object contains:
\describe{
\item{\code{fit}}{The \code{CmdStanMCMC} object from cmdstanr.}
\item{\code{stan_data}}{The list passed to Stan's \verb{$sample()}.}
\item{\code{hbb_data}}{The full \code{hbb_data} object for downstream
methods.}
\item{\code{formula}}{The \code{hbb_formula} object.}
\item{\code{prior}}{The \code{hbb_prior} object used.}
\item{\code{model_type}}{Character: one of \code{"base"}, \code{"weighted"},
\code{"svc"}, \code{"svc_weighted"}.}
\item{\code{model_name}}{Character: the Stan model file name (e.g.,
\code{"hbb_base"}).}
\item{\code{call}}{The matched call.}
\item{\code{elapsed}}{Numeric: wall-clock time in seconds.}
}
}
\description{
Fits a two-part hurdle Beta-Binomial model using Hamiltonian Monte
Carlo (HMC) via CmdStan. The model comprises an extensive margin
(Bernoulli for whether a center serves infant/toddler children) and
an intensive margin (zero-truncated Beta-Binomial for the enrollment
share among servers). Both margins share the same design matrix with
separate coefficient vectors.
}
\details{
\subsection{Model variants}{

The Stan model selected depends on the formula and whether survey
weights are provided:
\tabular{lll}{
\strong{Model} \tab \strong{Formula pattern} \tab \strong{Weights} \cr
\code{hbb_base}         \tab \code{y | trials(n) ~ x1 + x2} \tab No \cr
\code{hbb_weighted}     \tab same \tab Yes \cr
\code{hbb_svc}          \tab \code{... + (x1 | group)} \tab No \cr
\code{hbb_svc_weighted} \tab \code{... + (x1 | group)} \tab Yes \cr
}

Note that random-intercept-only models (\code{(1 | state_id)}) currently
use the \code{base} or \code{weighted} variants, as the random intercept is
absorbed into the SVC framework only when random slopes are present.
}

\subsection{Workflow}{

\code{hbb()} performs the following steps:
\enumerate{
\item Parses the formula (if a raw formula rather than an \code{hbb_formula}).
\item Validates the prior specification and MCMC arguments.
\item Prepares Stan data via \code{\link[=prepare_stan_data]{prepare_stan_data()}}.
\item Maps the prepared data and priors to the Stan data block.
\item Compiles the appropriate Stan model (cached after first use).
\item Runs MCMC sampling via CmdStan.
\item Checks for MCMC pathologies (divergences, treedepth, E-BFMI).
\item Returns an \code{hbb_fit} object for downstream analysis.
}
}

\subsection{Compilation}{

Stan models are compiled on first use and cached persistently. See
\code{\link[=hbb_compile]{hbb_compile()}} for details on the caching strategy and explicit
pre-compilation.
}
}
\examples{
\dontrun{
data(nsece_synth_small, package = "hurdlebb")

# Base model (no weights, no random effects)
fit1 <- hbb(
  y | trials(n_trial) ~ poverty + urban,
  data = nsece_synth_small,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
print(fit1)

# Weighted model
fit2 <- hbb(
  y | trials(n_trial) ~ poverty + urban,
  data = nsece_synth_small,
  weights = "weight",
  chains = 2, iter_warmup = 500, iter_sampling = 500
)

# SVC model with policy moderators
data(nsece_state_policy, package = "hurdlebb")
fit3 <- hbb(
  y | trials(n_trial) ~ poverty + urban +
    (poverty + urban | state_id) +
    state_level(mr_pctile),
  data = nsece_synth_small,
  state_data = nsece_state_policy,
  weights = "weight",
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
}

}
\seealso{
\code{\link[=hbb_formula]{hbb_formula()}}, \code{\link[=prepare_stan_data]{prepare_stan_data()}}, \code{\link[=hbb_prior]{hbb_prior()}},
\code{\link[=hbb_compile]{hbb_compile()}}, \code{\link[=is.hbb_fit]{is.hbb_fit()}}

Other fitting: 
\code{\link{is.hbb_fit}()},
\code{\link{print.hbb_fit}()}
}
\concept{fitting}
