% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{hbb_formula}
\alias{hbb_formula}
\title{Parse a Hurdle Beta-Binomial Model Formula}
\usage{
hbb_formula(formula)
}
\arguments{
\item{formula}{A two-sided formula of the form
\code{y | trials(n) ~ predictors}. See \strong{Details} for the full syntax.}
}
\value{
An S3 object of class \code{"hbb_formula"} with components:
\describe{
\item{\code{response}}{Character. The response variable name.}
\item{\code{trials}}{Character. The trials variable name.}
\item{\code{fixed}}{Character vector of fixed effect term names,
\strong{excluding} the intercept (which is always implicit).}
\item{\code{group}}{Character or \code{NULL}. The grouping variable for
random effects.}
\item{\code{random}}{Character vector of random effect terms, or \code{NULL}
if no random effects. Contains \code{"1"} for intercept-only random
effects, or variable names for state-varying coefficients.}
\item{\code{policy}}{Character vector of policy moderator term names,
or \code{NULL} if none specified.}
\item{\code{formula}}{The original formula object.}
\item{\code{svc}}{Logical. \code{TRUE} if the model includes random slopes
(state-varying coefficients), not just a random intercept.}
}
}
\description{
Converts a brms-style formula into a structured specification for a
two-part hurdle Beta-Binomial model. The fixed effects specified in
the formula enter \strong{both} model margins (extensive and intensive)
with separate coefficient vectors \eqn{\alpha} and \eqn{\beta}.
}
\details{
\subsection{Formula syntax}{

The left-hand side \strong{must} specify both the response variable and
the trials variable using the \verb{| trials()} syntax:

\if{html}{\out{<div class="sourceCode">}}\preformatted{y | trials(n_trial) ~ ...
}\if{html}{\out{</div>}}

The right-hand side supports four types of terms:
\enumerate{
\item \strong{Fixed effects}: Standard R formula terms (e.g., \code{x1 + x2}).
An intercept is always included automatically. Use \code{y | trials(n) ~ 1}
for an intercept-only model.
\item \strong{Random intercept}: \code{(1 | group)} specifies a random intercept
grouped by \code{group}.
\item \strong{State-varying coefficients (SVC)}: \code{(x1 + x2 | group)} specifies
random slopes for \code{x1} and \code{x2} (plus the implicit random intercept).
All random slope variables must also appear as fixed effects.
\item \strong{Policy moderators}: \code{state_level(v1 + v2)} specifies cross-level
interaction terms that predict the random effects. These require
that random effects are also specified.
}
}

\subsection{Model mapping}{

The parsed formula maps to the model as follows:
\itemize{
\item Fixed effects \eqn{\to} the \eqn{X} matrix (with auto-prepended
intercept column)
\item The grouping variable \eqn{\to} the state index vector
\item Policy moderators \eqn{\to} the \eqn{V} matrix (with auto-prepended
intercept column)
\item Both hurdle margins share the same \eqn{X} design matrix
}
}
}
\examples{
# Intercept-only model
f1 <- hbb_formula(y | trials(n_trial) ~ 1)
f1

# Fixed effects only
f2 <- hbb_formula(y | trials(n_trial) ~ poverty + urban)
f2

# Random intercept
f3 <- hbb_formula(y | trials(n_trial) ~ poverty + (1 | state_id))
f3

# SVC with policy moderators
f4 <- hbb_formula(
  y | trials(n_trial) ~ poverty + urban +
    (poverty + urban | state_id) +
    state_level(mr_pctile + tiered_reim)
)
f4

}
\seealso{
\code{\link[=validate_hbb_formula]{validate_hbb_formula()}} to check a formula against a dataset.

Other formula: 
\code{\link{validate_hbb_formula}()}
}
\concept{formula}
