% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{rztbetabinom}
\alias{rztbetabinom}
\title{Random generation from the Zero-Truncated Beta-Binomial}
\usage{
rztbetabinom(nn, n, mu, kappa)
}
\arguments{
\item{nn}{Number of draws to generate (positive integer).}

\item{n}{Integer vector of trial sizes (\eqn{n \ge 1}), recycled to
length \code{nn}.}

\item{mu}{Numeric vector of means in \eqn{(\varepsilon, 1 - \varepsilon)},
recycled to length \code{nn}.}

\item{kappa}{Numeric vector of concentrations (\eqn{> 0}), recycled to
length \code{nn}.}
}
\value{
An integer vector of length \code{nn}, each element in
\eqn{\{1, \ldots, n_i\}}.
}
\description{
Generates random draws from the zero-truncated Beta-Binomial via
rejection sampling on the standard Beta-Binomial.
}
\details{
The function is vectorised over \code{n}, \code{mu}, and \code{kappa}
using a parameter-grouping strategy. Unique parameter combinations are
identified, and rejection sampling is performed once per group with
smart batch sizing based on the analytical \eqn{p_0}.
}
\examples{
set.seed(42)
x <- rztbetabinom(1000, n = 10, mu = 0.3, kappa = 5)
min(x)  # always >= 1
mean(x)

# Vectorised parameters
rztbetabinom(6, n = c(5, 10, 20), mu = c(0.2, 0.5, 0.8), kappa = c(3, 10, 50))

}
\references{
Ghosal, S., Ghosh, S., and Moores, M. (2020).
\dQuote{Hierarchical beta-binomial models for batch effects in
cytometry data.} \emph{Journal of the Royal Statistical Society:
Series A}, \strong{183}(4), 1579--1601.
}
\seealso{
Other distributions: 
\code{\link{compute_p0}()},
\code{\link{compute_ztbb_mean}()},
\code{\link{dbetabinom}()},
\code{\link{dhurdle_betabinom}()},
\code{\link{dztbetabinom}()},
\code{\link{hurdle_mean}()},
\code{\link{hurdle_variance}()},
\code{\link{pbetabinom}()},
\code{\link{rbetabinom}()},
\code{\link{rhurdle_betabinom}()}
}
\concept{distributions}
