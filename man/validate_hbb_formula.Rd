% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{validate_hbb_formula}
\alias{validate_hbb_formula}
\title{Validate an hbb_formula Against a Dataset}
\usage{
validate_hbb_formula(object, data, state_data = NULL)
}
\arguments{
\item{object}{An object of class \code{"hbb_formula"}, as returned by
\code{\link[=hbb_formula]{hbb_formula()}}.}

\item{data}{A data frame containing provider-level variables
(response, trials, fixed effects, grouping variable).}

\item{state_data}{An optional data frame containing state-level
policy variables. Required if \code{object$policy} is not \code{NULL}.
Must contain a column matching \code{object$group} for merging.}
}
\value{
Invisibly returns \code{TRUE} if all checks pass. Throws an
informative error otherwise.
}
\description{
Checks that all variables referenced in the formula exist in the
supplied data frame(s) and that they have appropriate types and
ranges. This is typically called internally by the fitting function,
but can be used directly for early error checking.
}
\details{
The following checks are performed:
\enumerate{
\item \code{object} is an \code{hbb_formula}.
\item \code{data} is a data frame with at least one row.
\item The response variable exists and contains non-negative integers.
\item The trials variable exists and contains positive integers.
\item Response does not exceed trials for any observation.
\item All fixed effect variables exist and are numeric.
\item The grouping variable (if any) exists and is coercible to
integer.
\item If \code{state_data} is supplied: all policy variables exist in
\code{state_data}, the grouping variable exists in \code{state_data}, and
policy variables are numeric.
}
}
\examples{
f <- hbb_formula(y | trials(n_trial) ~ poverty + urban)
data(nsece_synth_small)
validate_hbb_formula(f, nsece_synth_small)

}
\seealso{
\code{\link[=hbb_formula]{hbb_formula()}}

Other formula: 
\code{\link{hbb_formula}()}
}
\concept{formula}
