% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{prepare_stan_data}
\alias{prepare_stan_data}
\title{Prepare Stan Data for a Hurdle Beta-Binomial Model}
\usage{
prepare_stan_data(
  formula,
  data,
  weights = NULL,
  stratum = NULL,
  psu = NULL,
  state_data = NULL,
  prior = NULL,
  center = TRUE,
  scale = TRUE
)
}
\arguments{
\item{formula}{An object of class \code{"hbb_formula"} (from \code{\link[=hbb_formula]{hbb_formula()}}),
or a raw formula that will be parsed via \code{hbb_formula()}.}

\item{data}{A data frame containing provider-level variables: the
response, trials, fixed effects, and optionally the grouping variable.}

\item{weights}{Character string naming the column in \code{data} containing
survey sampling weights, or \code{NULL} (default) for unweighted analysis.}

\item{stratum}{Character string naming the column in \code{data} containing
sampling stratum identifiers, or \code{NULL}.}

\item{psu}{Character string naming the column in \code{data} containing
primary sampling unit (PSU) identifiers, or \code{NULL}.}

\item{state_data}{A data frame of group-level (state-level) variables
for policy moderators. Required if \code{formula$policy} is not \code{NULL}.
Must contain a column matching the grouping variable for merging.}

\item{prior}{A prior specification list, or \code{NULL} (default) to use
package defaults. (Prior specification API is under development.)}

\item{center}{Logical. If \code{TRUE} (default), subtract column means from
non-intercept columns of X and V. The means are stored in the output
for back-transformation.}

\item{scale}{Logical. If \code{TRUE} (default), divide non-intercept columns
of X and V by their standard deviations after centering. The SDs are
stored in the output for back-transformation.}
}
\value{
An S3 object of class \code{"hbb_data"} (a list). See \strong{Value} for
full field descriptions.
}
\description{
Converts a formula specification and data frame into a validated list
suitable for passing to Stan. Constructs design matrices with optional
standardisation, builds group indices, normalises survey weights, and
computes all required dimensions.
}
\section{Value fields}{

\describe{
\item{\code{N}}{Integer. Number of observations.}
\item{\code{P}}{Integer. Number of provider covariates including intercept.}
\item{\code{S}}{Integer. Number of groups (states). 1 if no grouping.}
\item{\code{Q}}{Integer. Number of policy covariates including intercept.}
\item{\code{N_pos}}{Integer. Number of positive observations (z == 1).}
\item{\code{K}}{Integer. Random effects dimension per group.}
\item{\code{y}}{Integer vector of length N. Response counts.}
\item{\code{n_trial}}{Integer vector of length N. Trial sizes.}
\item{\code{z}}{Integer vector of length N. Participation indicators.}
\item{\code{X}}{Numeric matrix N x P. Design matrix (column 1 = intercept).}
\item{\code{state}}{Integer vector of length N. Group indices 1..S.}
\item{\code{V}}{Numeric matrix S x Q, or \code{NULL} if no groups.}
\item{\code{idx_pos}}{Integer vector. Row indices where z == 1.}
\item{\code{w_tilde}}{Numeric vector of normalised weights, or \code{NULL}.}
\item{\code{stratum_idx}}{Integer vector of stratum indices, or \code{NULL}.}
\item{\code{psu_idx}}{Integer vector of PSU indices, or \code{NULL}.}
\item{\code{n_strata}}{Integer. Number of unique strata, or \code{NULL}.}
\item{\code{n_psu}}{Integer. Number of unique PSUs, or \code{NULL}.}
\item{\code{x_center}}{Named numeric vector of column means, or \code{NULL}.}
\item{\code{x_scale}}{Named numeric vector of column SDs, or \code{NULL}.}
\item{\code{v_center}}{Named numeric vector of V column means, or \code{NULL}.}
\item{\code{v_scale}}{Named numeric vector of V column SDs, or \code{NULL}.}
\item{\code{formula}}{The \code{hbb_formula} object.}
\item{\code{prior}}{The prior specification (or \code{NULL}).}
\item{\code{group_levels}}{Character vector of original group labels.}
\item{\code{model_type}}{Character. One of \code{"base"}, \code{"weighted"},
\code{"svc"}, \code{"svc_weighted"}.}
}
}

\examples{
# Minimal example with the small synthetic dataset
data(nsece_synth_small, package = "hurdlebb")
f <- hbb_formula(y | trials(n_trial) ~ poverty + urban)
d <- prepare_stan_data(f, nsece_synth_small)
d

# With grouping and weights
f2 <- hbb_formula(
  y | trials(n_trial) ~ poverty + urban + (1 | state_id)
)
d2 <- prepare_stan_data(f2, nsece_synth_small, weights = "weight")
d2

# Full SVC model with policy moderators
data(nsece_state_policy, package = "hurdlebb")
f3 <- hbb_formula(
  y | trials(n_trial) ~ poverty + urban +
    (poverty + urban | state_id) +
    state_level(mr_pctile)
)
d3 <- prepare_stan_data(
  f3, nsece_synth_small,
  weights = "weight", state_data = nsece_state_policy
)
d3

}
\seealso{
\code{\link[=hbb_formula]{hbb_formula()}}, \code{\link[=validate_hbb_data]{validate_hbb_data()}}

Other data: 
\code{\link{validate_hbb_data}()}
}
\concept{data}
