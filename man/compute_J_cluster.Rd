% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{compute_J_cluster}
\alias{compute_J_cluster}
\title{Compute the Cluster-Robust Meat Matrix}
\usage{
compute_J_cluster(scores, hbb_data)
}
\arguments{
\item{scores}{Numeric matrix of dimension \eqn{N \times D}. The
posterior mean score matrix from \code{\link[=compute_score_matrix]{compute_score_matrix()}}.}

\item{hbb_data}{An S3 object of class \code{"hbb_data"} (from
\code{\link[=prepare_stan_data]{prepare_stan_data()}}) or a list containing the fields
\code{stratum_idx}, \code{psu_idx}, \code{w_tilde}, and \code{N}.}
}
\value{
Numeric matrix of dimension \eqn{D \times D}. The
cluster-robust meat matrix (positive semi-definite).
}
\description{
Aggregates weighted scores to the stratum--PSU level and computes
the Taylor linearisation variance estimator with finite population
correction (FPC) at the stratum level.
}
\details{
\subsection{Algorithm}{

For each stratum \eqn{h} with \eqn{C_h} PSUs:
\enumerate{
\item Compute weighted score totals per PSU:
\eqn{s_{hc} = \sum_{i \in \mathrm{PSU}(h,c)} \tilde{w}_i s_i}
\item Compute stratum mean: \eqn{\bar{s}_h = C_h^{-1} \sum_c s_{hc}}
\item Center: \eqn{\delta_{hc} = s_{hc} - \bar{s}_h}
\item Accumulate with FPC: \eqn{J_h = \frac{C_h}{C_h - 1}
   \sum_c \delta_{hc} \delta_{hc}^\top}
}

Uses \code{rowsum()} for efficient PSU-level aggregation.
Singleton strata (\eqn{C_h = 1}) are skipped with a warning.
}
}
\seealso{
\code{\link[=sandwich_variance]{sandwich_variance()}}

Other sandwich: 
\code{\link{compute_H_obs}()},
\code{\link{compute_der}()},
\code{\link{compute_score_matrix}()},
\code{\link{print.hbb_sandwich}()},
\code{\link{sandwich_variance}()}
}
\concept{sandwich}
