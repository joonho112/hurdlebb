% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandwich.R
\name{compute_H_obs}
\alias{compute_H_obs}
\title{Compute the Block-Diagonal Observed Information Matrix}
\usage{
compute_H_obs(fit, scores = NULL)
}
\arguments{
\item{fit}{An object of class \code{"hbb_fit"}.}

\item{scores}{Optional \eqn{N \times D} score matrix (as from
\code{\link[=compute_score_matrix]{compute_score_matrix()}}). If \code{NULL}, computed internally.}
}
\value{
A list with components:
\describe{
\item{\code{H_obs}}{Numeric matrix (\eqn{D \times D}). Block-diagonal
observed information.}
\item{\code{H_obs_inv}}{Numeric matrix (\eqn{D \times D}). Inverse of
H_obs (the bread matrix).}
\item{\code{H_ext}}{Numeric matrix (\eqn{P \times P}). Extensive-margin
Fisher information block.}
\item{\code{H_int}}{Numeric matrix (\eqn{(P+1) \times (P+1)}).
Intensive + kappa block from information identity.}
\item{\code{ridge_applied}}{Logical. Whether ridge regularisation was
needed.}
}
}
\description{
Computes the observed information matrix \eqn{H_{\mathrm{obs}}} for
the fixed effects of a hurdle Beta-Binomial model. Uses a
block-diagonal structure:
}
\details{
\itemize{
\item \strong{Extensive margin} (alpha block, \eqn{P \times P}): analytic
Fisher information from logistic regression.
\item \strong{Intensive + kappa block} (\eqn{(P+1) \times (P+1)}): empirical
information identity using posterior mean scores.
}
}
\section{Extensive margin (H_ext)}{

For the base weighted model:
\deqn{H_{\mathrm{ext}} = \sum_{i=1}^N \tilde{w}_i\, q_i (1-q_i)\,
  X_i X_i^\top}
where \eqn{q_i = \mathrm{logit}^{-1}(X_i^\top \hat{\alpha})}.

For SVC models, the linear predictor includes state random effects:
\eqn{q_i = \mathrm{logit}^{-1}(X_i^\top \hat{\alpha} + X_i^\top
\hat{\delta}^{\mathrm{ext}}_{s[i]})}.
}

\section{Intensive margin (H_int)}{

The zero-truncated Beta-Binomial Hessian is analytically complex,
so we use the information identity:
\deqn{H_{\mathrm{int}} = \sum_{i=1}^N \tilde{w}_i\,
  s_i^{\mathrm{int}} (s_i^{\mathrm{int}})^\top}
where \eqn{s_i^{\mathrm{int}} = (s_{\beta,i}, s_{\kappa,i})^\top}
is the \eqn{(P+1)}-dimensional intensive score at the posterior mean.
}

\seealso{
\code{\link[=sandwich_variance]{sandwich_variance()}}, \code{\link[=compute_score_matrix]{compute_score_matrix()}}

Other sandwich: 
\code{\link{compute_J_cluster}()},
\code{\link{compute_der}()},
\code{\link{compute_score_matrix}()},
\code{\link{print.hbb_sandwich}()},
\code{\link{sandwich_variance}()}
}
\concept{sandwich}
