% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{hbb_prior}
\alias{hbb_prior}
\title{Specify Priors for a Hurdle Beta-Binomial Model}
\usage{
hbb_prior(
  alpha = NULL,
  beta = NULL,
  log_kappa = NULL,
  gamma = NULL,
  tau = NULL,
  lkj_eta = NULL
)
}
\arguments{
\item{alpha}{Prior on the extensive-margin fixed effects
\eqn{\alpha} (logit scale). A named list with elements \code{dist}
(character), \code{mean} (numeric scalar), and \code{sd} (positive numeric
scalar). Currently only \code{dist = "normal"} is supported.}

\item{beta}{Prior on the intensive-margin fixed effects \eqn{\beta}
(logit scale). Same structure as \code{alpha}.}

\item{log_kappa}{Prior on the log-concentration parameter
\eqn{\log \kappa}. Same structure as \code{alpha}.}

\item{gamma}{Prior on the policy moderator coefficients
\eqn{\Gamma}. Same structure as \code{alpha}.}

\item{tau}{Prior on the random effect scale parameters \eqn{\tau}.
Same structure as \code{alpha}. Note: in Stan, \eqn{\tau} is declared
with \code{lower = 0}, so a \code{Normal(mean, sd)} prior becomes a
\strong{half-normal} prior in practice. The \code{mean} component is
typically set to 0 for half-normal priors.}

\item{lkj_eta}{Concentration parameter \eqn{\eta > 0} for the
LKJ prior on the Cholesky factor of the correlation matrix
\eqn{L_\Omega}. Higher values shrink the correlation toward the
identity matrix. A scalar \eqn{\eta = 1} gives a uniform prior
over correlation matrices; \eqn{\eta = 2} (default) mildly favours
the identity.}
}
\value{
An S3 object of class \code{"hbb_prior"} with components:
\describe{
\item{\code{alpha}}{Named list with \code{dist}, \code{mean}, \code{sd}.}
\item{\code{beta}}{Named list with \code{dist}, \code{mean}, \code{sd}.}
\item{\code{log_kappa}}{Named list with \code{dist}, \code{mean}, \code{sd}.}
\item{\code{gamma}}{Named list with \code{dist}, \code{mean}, \code{sd}.}
\item{\code{tau}}{Named list with \code{dist}, \code{mean}, \code{sd}.}
\item{\code{lkj_eta}}{Positive numeric scalar.}
}
}
\description{
Creates a prior specification object that controls the prior
distributions used in the Stan model. All arguments are optional;
unspecified components receive the defaults from \code{\link[=default_prior]{default_prior()}}.
}
\details{
\subsection{Extensibility}{

The \code{dist} field in each component currently accepts only \code{"normal"}.
The structure is designed to accommodate future distributions (e.g.,
Student-t) without breaking the API.
}

\subsection{Model-specific usage}{

Not all parameters are present in every model variant:
\itemize{
\item \strong{hbb_base} and \strong{hbb_weighted}: use \code{alpha}, \code{beta},
\code{log_kappa} only.
\item \strong{hbb_svc} and \strong{hbb_svc_weighted}: additionally use
\code{gamma}, \code{tau}, and \code{lkj_eta}.
}
Unused prior components are silently ignored by the fitting function.
}
}
\examples{
# Default priors
default_prior()

# Tighter prior on fixed effects
hbb_prior(alpha = list(dist = "normal", mean = 0, sd = 1))

# Custom prior on dispersion and LKJ
hbb_prior(
  log_kappa = list(dist = "normal", mean = 3, sd = 1),
  lkj_eta   = 4
)

# Override everything
p <- hbb_prior(
  alpha     = list(dist = "normal", mean = 0, sd = 1),
  beta      = list(dist = "normal", mean = 0, sd = 1),
  log_kappa = list(dist = "normal", mean = 2, sd = 1),
  gamma     = list(dist = "normal", mean = 0, sd = 0.5),
  tau       = list(dist = "normal", mean = 0, sd = 0.5),
  lkj_eta   = 5
)
print(p)

}
\seealso{
\code{\link[=default_prior]{default_prior()}} for the package defaults.

Other priors: 
\code{\link{default_prior}()}
}
\concept{priors}
