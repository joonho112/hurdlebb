% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan-cache.R
\name{hbb_compile}
\alias{hbb_compile}
\title{Compile Stan Models for hurdlebb}
\usage{
hbb_compile(
  model = NULL,
  force = FALSE,
  quiet = FALSE,
  cpp_options = list(),
  stanc_options = list()
)
}
\arguments{
\item{model}{Character vector of model names to compile, or \code{NULL}
(default) to compile all four models. Valid names: \code{"hbb_base"},
\code{"hbb_weighted"}, \code{"hbb_svc"}, \code{"hbb_svc_weighted"}.}

\item{force}{Logical. If \code{TRUE}, recompile even when the cached
executable is up-to-date. Default \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages
during compilation. Default \code{FALSE}.}

\item{cpp_options}{Named list of C++ compilation options passed to
\code{cmdstanr::cmdstan_model()}. For example,
\code{list(stan_threads = TRUE)} enables within-chain threading.
Default \code{list()}.}

\item{stanc_options}{Named list of stanc3 transpiler options.
Default \code{list()}.}
}
\value{
Invisibly returns a named list of compiled \code{CmdStanModel}
objects.
}
\description{
Compiles (or retrieves from cache) the Stan models shipped with the
package. Compiled executables are stored in a persistent user-level
cache directory so that compilation happens only once per Stan source
version.
}
\details{
You can call this function explicitly, or let the main fitting
function trigger compilation automatically on first use.
\subsection{Cache location}{

Compiled executables are stored under
\code{tools::R_user_dir("hurdlebb", "cache")}, following R >= 4.0
conventions. Each model produces two files in the cache directory:
the compiled executable and a \code{.md5} file recording the MD5 digest
of the Stan source at compilation time.
}

\subsection{Hash-based invalidation}{

When a Stan source file changes (e.g., after a package update), the
MD5 hash no longer matches the cached \code{.md5} file, triggering
automatic recompilation.
}

\subsection{Thread safety}{

A simple file-based lock mechanism prevents two concurrent R sessions
from compiling the same model simultaneously. If a lock file is
detected, the function polls and waits (with a configurable timeout).
Stale locks from dead processes are broken automatically.
}
}
\examples{
\dontrun{
# Compile all models (one-time operation)
hbb_compile()

# Compile only the base model with threading support
hbb_compile("hbb_base", cpp_options = list(stan_threads = TRUE))

# Force recompilation after CmdStan upgrade
hbb_compile(force = TRUE)
}
}
\seealso{
\code{\link{hbb_clear_cache}} to remove all cached executables.
}
